<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cantinaunitbv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/c"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/c"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PreWelcomeScreen"
            android:theme="@style/PreWelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Logare"></activity>
        <activity android:name=".WelcomeScreen" />

        <activity
            android:name=".pas2"
            android:label="@string/title_activity_pas2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>